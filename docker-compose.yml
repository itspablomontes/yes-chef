services:
  api:
    build: .
    user: "0:0"
    ports:
      - "8000:8000"
    env_file: .env
    environment:
      DATABASE_URL: sqlite+aiosqlite:////app/data/yeschef.db
      OPENAI_MODEL: gpt-4o-mini
    volumes:
      - ./data:/app/data
